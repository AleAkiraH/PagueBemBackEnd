DynamoDB module for application data

Creates a DynamoDB table for application use (not to be confused with the backend lock table created by the backend module).

Usage:
- Deploy via the Deploy workflow with component `dynamodb` or run `terraform apply` in this folder.
